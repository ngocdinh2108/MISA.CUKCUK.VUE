var enumeration = enumeration || {}

// Các mode của form detail
enumeration.FormMode = {
    Add: 1, // Thêm mới
    Edit: 2, // Sửa
    Delete: 3 // Xóa
}

// Giới tính
enumeration.Gender = {
    Female: 0, // Nữ
    Male: 1, // Nam
    Other: 2 // Không xác định
}

// Tình trạng làm việc
enumeration.WorkStatus = {
    Active: 0,
    UnActive: 1,
    Pending: 2,
}